export interface DomainSave {
  uid: string;
  title: string;
  bookId: string;
}

export interface DomainGet {
  uid: string;
  title: string;
  bookId: string;
}

export interface DomainDelete {
  uid: string;
  title: string;
  bookId: string;
}

export interface WebSave {
  uid: string;
  title: string;
  bookId: string;
}

export interface WebDelete {
  uid: string;
  title: string;
  bookId: string;
}
